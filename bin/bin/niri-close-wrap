#!/bin/bash

output=$(niri msg focused-window)

app_id_line=$(echo "$output" | grep "App ID:")

if [[ "$app_id_line" == *"App ID: \"org.freedesktop.Xwayland\""* ]]; then
    notify-send "Niri" "Use Ctrl+Shift+Super+Q to close this rootful Xwayland"
else
    niri msg action close-window
fi

